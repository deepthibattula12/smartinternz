import numpy as np
array=np.random.rand(3,3)
mean=np.mean(array)
median=np.median(array)
std=np.std(array)
print("The mean is %.2f, the median is %.2f and standard deviation
is %.2f"%(mean,median,std))
